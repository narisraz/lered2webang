import Credential from "../interfaces/Credential";

const credentials: Credential[] = [
  {
    userId: '1',
    email: 'naris.razafimahatratra@gmail.com',
    password: 'bob.12'
  }
]

export default credentials
